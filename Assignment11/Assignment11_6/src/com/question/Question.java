


/*
6. Store Students in LinkedHashMap<> so that, for given roll, 
	Student can be searched in fastest possible time. 
	Do we need to write equals() and
	hashCode() in Student class? 
	Follow menu-driven approach. Hint:
	class Student {
	// ...
	}
	Map<Integer, Student> map = new HashMap<>();
	s = new Student();
	acceptStudent(s); // implement method in Main class
	map.put(s.getRoll(), s);
	roll = sc.nextInt();
	s = map.get(roll);
	
*/